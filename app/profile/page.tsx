import PlaceholderScreen from "@/components/PlaceholderScreen";

export const metadata = { title: "Profile" };

export default function ProfilePage() {
  return (
    <PlaceholderScreen
      title="Profile"
      subtitle="Soon: sign in, set your sensitivity preferences, and manage your saved products and places."
    />
  );
}
