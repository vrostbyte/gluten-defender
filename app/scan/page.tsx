import PlaceholderScreen from "@/components/PlaceholderScreen";

export const metadata = { title: "Scan" };

export default function ScanPage() {
  return (
    <PlaceholderScreen
      title="Scan"
      subtitle="Soon: point your camera at a product barcode to get a clear gluten-safety verdict — with the reasoning behind it."
    />
  );
}
